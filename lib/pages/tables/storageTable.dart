import 'package:fastfood/navBar.dart';
import 'package:flutter/material.dart';

class StorageTable extends StatelessWidget {
  const StorageTable({super.key});

  @override
  Widget build(BuildContext context) {
    return NavBarWidget(child: Text('Storage Table Placeholder'));
  }
}